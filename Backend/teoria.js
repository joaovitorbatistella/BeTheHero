/**
 * Rota / Recursos
 */

 /**
  * Métodos HTTP:
  * 
  * GET: Buscar/listar uma informação no back-end;
  * POST: Criar uma informação no back-end;
  * PUT: Alterar um ainformação no back-end;
  * DELETE: Deletar uma informação no back-end;
  */

  /**
   * Tipos de parâmetros:
   * 
   * Query Params: Parâmetos nomeados enviados na rota após "?" (Filtros, paginação);
   * Route Params: Parâmetros utilizados para identificr recursos;
   * Request Boddy: Corpo da requiseção, utilizado para criar ou alterar recursos;
   */

   /**
    * SQL: MySQL, SQLite, PostgreSQL, Oracle, Microsoft SQL Server;
    * NoSQL: MongoDB, CoucDB, etc;
    */

    /**
     * Driver: SELECT * FROM users;
     * Querry Builder: table('users').select('*').where();
     */